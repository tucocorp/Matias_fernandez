<%= form_for(@unit_work) do |f| %>
  <% if @unit_work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unit_work.errors.count, "error") %> prohibited this unit_work from being saved:</h2>

      <ul>
      <% @unit_work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.datetime_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.datetime_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :activity_id %><br>
    <%= f.number_field :activity_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
